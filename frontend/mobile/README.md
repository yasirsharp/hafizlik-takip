# Hafızlık Takip — Mobil Uygulama (React Native + Expo)

> Bu klasör, React Native + Expo ile geliştirilecek mobil uygulamayı içerir.
> Detaylı geliştirme kuralları için `../DEVELOPMENT_RULES.md` dosyasına bakın.

## Başlatma (İleri Fazlarda)

```bash
npx -y create-expo-app@latest ./
```
